"use client";

import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { calculateAllFares } from "../lib/fareRules";
import { buildWhatsAppLink } from "../lib/contact";
import { supabase } from "../lib/supabaseClient";
import { theme } from "../lib/theme";
import PageHeader from "../components/PageHeader";
import { validateCapacity } from "../../lib/capacityValidation";

const VOYNU_UPI_VPA = "voynu@upi";
function IconCheck({ size = 14 }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><path d="M20 6L9 17l-5-5" /></svg>; }
function IconWhatsApp({ size = 18 }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor"><path d="M12 2a10 10 0 0 0-8.6 15L2 22l5.2-1.4A10 10 0 1 0 12 2zm0 18.2a8.1 8.1 0 0 1-4.2-1.2l-.3-.2-3.1.8.8-3-.2-.3A8.2 8.2 0 1 1 12 20.2zm4.5-6.1c-.2-.1-1.5-.7-1.7-.8-.2-.1-.4-.1-.6.1s-.7.8-.9 1c-.2.2-.3.2-.5.1a6.7 6.7 0 0 1-2-1.2 7.4 7.4 0 0 1-1.4-1.7c-.1-.2 0-.4.1-.5l.4-.4c.1-.1.2-.3.2-.4a.5.5 0 0 0 0-.5c-.1-.1-.6-1.5-.9-2-.2-.5-.4-.4-.6-.4h-.5a1 1 0 0 0-.7.3 3 3 0 0 0-1 2.3c0 1.3 1 2.6 1.1 2.8.1.2 2 3.1 4.9 4.3a16 16 0 0 0 1.6.6 3.9 3.9 0 0 0 1.8.1c.5-.1 1.5-.6 1.7-1.2.2-.6.2-1.1.2-1.2-.1-.1-.3-.2-.5-.3z" /></svg>; }
function IconUsers({ size = 13 }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="9" cy="8" r="3.2" /><path d="M2.5 20c0-3.5 3-5.5 6.5-5.5s6.5 2 6.5 5.5" /><path d="M16 8.5a3 3 0 1 1 3.6 2.9" /><path d="M17.5 14.6c2.6.3 4 2 4 5.4" /></svg>; }
function IconCash({ size = 15 }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="2.5" y="6" width="19" height="12" rx="2.5" /><circle cx="12" cy="12" r="3" /></svg>; }
function IconUpi({ size = 15 }) { return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="4" y="3" width="16" height="18" rx="2.5" /><path d="M9 8h6M9 12h6M9 16h3" /></svg>; }

export default function CabSelectionPage() {
  const router = useRouter();
  const [booking, setBooking] = useState(null); const [loaded, setLoaded] = useState(false); const [selectedVehicleId, setSelectedVehicleId] = useState(null); const [passengerCount, setPassengerCount] = useState(1); const [luggageCount, setLuggageCount] = useState(0); const [paymentMethod, setPaymentMethod] = useState("cash"); const [upiPayClicked, setUpiPayClicked] = useState(false); const [upiPaymentConfirmed, setUpiPaymentConfirmed] = useState(false); const [isConfirming, setIsConfirming] = useState(false); const [capacityError, setCapacityError] = useState("");
  useEffect(() => { try { const raw = sessionStorage.getItem("voynu_booking"); if (raw) { const parsed = JSON.parse(raw); setBooking(parsed); setPassengerCount(Math.max(1, Number(parsed?.passengerCount) || 1)); setLuggageCount(Math.max(0, Number(parsed?.luggageCount) || 0)); } } catch (error) { console.error("VOYNU: unable to read booking data:", error); } finally { setLoaded(true); } }, []);
  useEffect(() => { if (loaded && !booking) router.replace("/"); }, [loaded, booking, router]);
  const fares = useMemo(() => { if (!booking || !Number.isFinite(Number(booking?.journey?.oneWayDistanceKm))) return []; return calculateAllFares({ oneWayDistanceKm: booking.journey.oneWayDistanceKm, tripType: booking.tripType }); }, [booking]);
  const eligibleFares = useMemo(() => fares.filter((fare) => validateCapacity({ passengerCount, luggageCount, passengerCapacity: fare.capacity, luggageCapacity: fare.luggageCapacity }).valid), [fares, passengerCount, luggageCount]);
  useEffect(() => { if (!eligibleFares.length) { setSelectedVehicleId(null); return; } if (!eligibleFares.some((fare) => fare.vehicleTypeId === selectedVehicleId)) setSelectedVehicleId(eligibleFares[0].vehicleTypeId); }, [eligibleFares, selectedVehicleId]);
  useEffect(() => { const result = selectedVehicleId ? eligibleFares.find((f) => f.vehicleTypeId === selectedVehicleId) : null; if (!result) setCapacityError(eligibleFares.length ? "Please select a suitable vehicle category." : "No vehicle category can accommodate this passenger and luggage requirement."); else setCapacityError(""); }, [eligibleFares, selectedVehicleId]);
  useEffect(() => { setUpiPayClicked(false); setUpiPaymentConfirmed(false); }, [selectedVehicleId, paymentMethod]);
  const selectedFare = eligibleFares.find((f) => f.vehicleTypeId === selectedVehicleId) || null;
  const isRoundTrip = booking?.tripType === "roundtrip";
  const buildConfirmationMessage = () => { if (!booking || !selectedFare) return ""; const paymentLine = paymentMethod === "upi" ? "UPI — customer confirmed payment sent (please verify before dispatch)" : "Pay on Pickup (Cash)"; const lines = ["New VOYNU booking request:", "", `Trip type: ${isRoundTrip ? "Round Trip" : "One Way"}`, `Pickup: ${booking.pickup?.name || ""}`, `Drop: ${booking.drop?.name || ""}`, `Distance: ${booking.journey?.oneWayDistanceText || ""}${isRoundTrip ? ` (round trip: ${booking.journey?.totalDistanceText || ""})` : ""}`, `Travel date: ${booking.travelDate || ""}`, `Pickup time: ${booking.pickupTime || ""}`]; if (isRoundTrip) lines.push(`Return date: ${booking.returnDate || ""}`, `Return time: ${booking.returnTime || ""}`); lines.push("", `Passengers: ${passengerCount}`, `Luggage: ${luggageCount}`, `Cab type: ${selectedFare.vehicleName}`, `Estimated fare: ₹${selectedFare.totalFare}`, `Payment: ${paymentLine}`, "", `Passenger: ${booking.passengerName || ""}`, `Phone: ${booking.phone || ""}`, `WhatsApp: ${booking.whatsapp || ""}`); return lines.join("\n"); };
  const upiHref = selectedFare ? `upi://pay?pa=${encodeURIComponent(VOYNU_UPI_VPA)}&pn=${encodeURIComponent("VOYNU")}&am=${selectedFare.totalFare}&cu=INR&tn=${encodeURIComponent("VOYNU Cab Booking")}` : "";
  const canConfirm = Boolean(selectedFare) && (paymentMethod === "cash" || (paymentMethod === "upi" && upiPaymentConfirmed));
  const handleConfirm = async () => { if (!selectedFare || !canConfirm || isConfirming) return; const capacity = validateCapacity({ passengerCount, luggageCount, passengerCapacity: selectedFare.capacity, luggageCapacity: selectedFare.luggageCapacity }); if (!capacity.valid) { setCapacityError(capacity.reason); return; } setIsConfirming(true); try { const { data: userData } = await supabase.auth.getUser(); const { data: sessionData } = await supabase.auth.getSession(); const idempotencyKey = globalThis.crypto?.randomUUID?.() || `${Date.now()}-${Math.random().toString(36).slice(2)}`; const response = await fetch("/api/bookings/create", { method: "POST", headers: { "Content-Type": "application/json", ...(sessionData?.session?.access_token ? { Authorization: `Bearer ${sessionData.session.access_token}` } : {}) }, body: JSON.stringify({ userId: userData?.user?.id || null, vehicleCategoryId: selectedFare.vehicleCategoryId || selectedFare.vehicleTypeId, passengerCount, luggageCount, paymentMethod, idempotencyKey, booking }) }); const result = await response.json(); if (!response.ok || !result?.booking?.id) throw new Error(result?.error || "Unable to create booking"); const confirmedBooking = { ...booking, passengerCount, luggageCount, selectedFare: { ...selectedFare, totalFare: result.booking.fare, serverBookingId: result.booking.id }, paymentMethod, bookingId: result.booking.id, paymentStatus: result.booking.payment_status, bookingStatus: result.booking.booking_status, confirmedAt: new Date().toISOString() }; try { sessionStorage.setItem("voynu_confirmed_booking", JSON.stringify(confirmedBooking)); sessionStorage.removeItem("voynu_booking"); } catch (error) { console.error("VOYNU: unable to save confirmed booking:", error); } window.open(buildWhatsAppLink(buildConfirmationMessage()), "_blank"); router.replace("/booking-confirmed"); } catch (error) { console.error("VOYNU: unable to create booking:", error); setCapacityError(error.message || "Unable to create booking. Please try again."); } finally { setIsConfirming(false); } };
  if (loaded && !booking) return null; if (!loaded || !booking) return <main className="loading"><div className="spinner" /></main>;
  return <main className="page"><PageHeader maxWidth={theme.maxWidth.content} whatsappHref={buildWhatsAppLink("Hi VOYNU, I have a question about my booking.")} /><div className="content"><div className="summaryCard"><div className="summaryRoute"><div className="routeDot routeDotPickup" /><div className="routeText">{booking.pickup?.name}</div></div><div className="routeLine" /><div className="summaryRoute"><div className="routeDot routeDotDrop" /><div className="routeText">{booking.drop?.name}</div></div><div className="summaryMeta"><span>{isRoundTrip ? "Round Trip" : "One Way"}</span><span>•</span><span>{booking.journey?.oneWayDistanceText}</span><span>•</span><span>{booking.travelDate} at {booking.pickupTime}</span></div></div><h2 className="sectionHeading">Passengers & luggage</h2><div className="requirementsCard"><label>Passengers<select value={passengerCount} onChange={(e) => setPassengerCount(Number(e.target.value))}>{Array.from({ length: 12 }, (_, i) => i + 1).map((n) => <option key={n} value={n}>{n}</option>)}</select></label><label>Luggage<select value={luggageCount} onChange={(e) => setLuggageCount(Number(e.target.value))}>{Array.from({ length: 11 }, (_, i) => i).map((n) => <option key={n} value={n}>{n}</option>)}</select></label></div><h2 className="sectionHeading">Choose your ride</h2><div className="cabList">{eligibleFares.map((fare) => <button key={fare.vehicleTypeId} type="button" className={selectedVehicleId === fare.vehicleTypeId ? "cabCard active" : "cabCard"} onClick={() => setSelectedVehicleId(fare.vehicleTypeId)}><div className="cabCardLeft"><div className="cabName">{fare.vehicleName}</div><div className="cabMeta"><IconUsers size={12} /><span>{fare.capacity} passengers</span><span className="cabDot">•</span><span>{fare.luggageCapacity} luggage</span><span className="cabDot">•</span><span>{fare.description}</span></div></div><div className="cabCardRight"><div className="cabPrice">₹{fare.totalFare}</div><div className={selectedVehicleId === fare.vehicleTypeId ? "cabRadio cabRadioActive" : "cabRadio"}>{selectedVehicleId === fare.vehicleTypeId && <IconCheck size={11} />}</div></div></button>)}</div>{capacityError && <div className="capacityError">{capacityError}</div>}<h2 className="sectionHeading">Payment</h2><div className="paymentGrid"><button type="button" className={paymentMethod === "cash" ? "paymentCard active" : "paymentCard"} onClick={() => setPaymentMethod("cash")}><IconCash size={16} /><span>Pay on Pickup</span></button><button type="button" className={paymentMethod === "upi" ? "paymentCard active" : "paymentCard"} onClick={() => setPaymentMethod("upi")}><IconUpi size={16} /><span>UPI</span></button></div>{paymentMethod === "upi" && selectedFare && <div className="upiFlow">{!upiPaymentConfirmed && <a href={upiHref} className="upiPayButton" onClick={() => setUpiPayClicked(true)}><IconUpi size={17} /><span>Pay ₹{selectedFare.totalFare} via UPI app</span></a>}{upiPayClicked && !upiPaymentConfirmed && <div className="upiConfirmRow"><p>Completed the payment in your UPI app?</p><div className="upiConfirmActions"><button type="button" className="upiConfirmYes" onClick={() => setUpiPaymentConfirmed(true)}><IconCheck size={12} />Yes, I've paid</button><button type="button" className="upiConfirmRetry" onClick={() => setUpiPayClicked(false)}>Didn't pay yet</button></div></div>}{upiPaymentConfirmed && <div className="upiConfirmedChip"><IconCheck size={13} /><span>Payment marked as completed</span></div>}</div>}{selectedFare && <div className="fareBreakdown"><div className="fareRow"><span>Base fare</span><span>₹{selectedFare.baseFare}</span></div><div className="fareRow"><span>Distance ({selectedFare.billedDistanceKm.toFixed(1)} km)</span><span>₹{selectedFare.distanceFare}</span></div>{selectedFare.driverAllowance > 0 && <div className="fareRow"><span>Driver allowance</span><span>₹{selectedFare.driverAllowance}</span></div>}<div className="fareRow fareRowTotal"><span>Total</span><span>₹{selectedFare.totalFare}</span></div></div>}{canConfirm && <button type="button" className="confirmButton" onClick={handleConfirm} disabled={isConfirming}><IconWhatsApp size={18} /><span>{isConfirming ? "Confirming..." : "Confirm on WhatsApp"}</span></button>}{!canConfirm && paymentMethod === "upi" && <p className="upiHint">Complete your UPI payment above to continue to WhatsApp confirmation.</p>}<p className="disclaimer">Fares shown are estimates. Final fare is confirmed by our team on WhatsApp before your ride is dispatched.</p></div><style jsx>{`* { box-sizing:border-box; }.page { min-height:100vh; background:#f5faf6; color:#16241d; font-family:'Plus Jakarta Sans',sans-serif; }.content { width:min(720px,calc(100% - 32px)); margin:0 auto; padding:24px 0 60px; }.loading { min-height:100vh; display:flex; align-items:center; justify-content:center; background:${theme.colors.bg}; }.spinner { width:34px; height:34px; border:3px solid rgba(8,120,63,.18); border-top-color:${theme.colors.primary}; border-radius:50%; }.summaryCard,.requirementsCard,.fareBreakdown { padding:18px; border-radius:16px; background:#fff; border:1px solid #e5ede8; box-shadow:0 12px 30px rgba(10,40,25,.06); }.summaryRoute { display:flex; align-items:flex-start; gap:11px; }`}</style></main>;
}