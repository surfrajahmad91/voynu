import LoginPage from "../../../../shared/auth/login";

export default function DriverLoginPage() {
  return (
    <LoginPage
      subtitle="Log in to manage your trips."
      panelDescription="Manage your assigned trips, update ride status as you go, and get paid — all from one place."
    />
  );
}
