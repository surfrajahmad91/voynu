name: VOYNU source separation v2
on: push
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Migrate and build
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f app/page.js ]; then echo 'Nothing to migrate.'; exit 0; fi
          test -d app/admin; test -f app/driver/page.js; test -f lib/capacityValidation.js
          mkdir -p apps/customer/app/components apps/customer/app/api/_lib apps/customer/app/lib apps/admin/app/lib apps/driver/app/components shared/components shared/lib shared/styles shared/api shared/auth
          move_replace() { src="$1"; dst="$2"; mkdir -p "$(dirname "$dst")"; rm -rf -- "$dst"; mv -- "$src" "$dst"; }
          for p in account booking-confirmed cab-selection signup; do move_replace "app/$p/page.js" "apps/customer/app/$p/page.js"; done
          move_replace app/page.js apps/customer/app/page.js
          for p in login forgot-password reset-password; do move_replace "app/$p/page.js" "shared/auth/$p.js"; done
          for f in AccountLink.jsx AuthShell.jsx LiveTripMap.js NotificationBell.jsx PageHeader.jsx PushNotifications.jsx; do move_replace "app/components/$f" "shared/components/$f"; done
          for f in AuthLanding.jsx DateTimeBookingGuard.jsx LocationPicker.jsx MapLocationPicker.jsx; do move_replace "app/components/$f" "apps/customer/app/components/$f"; done
          for f in supabaseClient.js theme.js; do move_replace "app/lib/$f" "shared/lib/$f"; done
          for f in contact.js fareRules.js geofence.js googleMaps.js serviceAreas.js tripRules.js; do move_replace "app/lib/$f" "apps/customer/app/lib/$f"; done
          move_replace lib/capacityValidation.js apps/customer/lib/capacityValidation.js
          while IFS= read -r src; do rel="${src#app/admin/}"; move_replace "$src" "apps/admin/app/admin/$rel"; done < <(find app/admin -type f | sort)
          move_replace app/lib/admin.js apps/admin/app/lib/admin.js
          move_replace app/driver/page.js apps/driver/app/driver/page.js
          move_replace app/components/DriverNavigationMode.js apps/driver/app/components/DriverNavigationMode.js
          move_replace app/api/_lib/sendBookingEmail.js apps/customer/app/api/_lib/sendBookingEmail.js
          move_replace app/api/bookings/create/route.js apps/customer/app/api/bookings/create/route.js
          move_replace app/api/_lib/roadDistance.ts shared/api/roadDistance.ts
          rm -rf -- app/api/route-distance app/api/route-map

          node <<'NODE'
          const fs=require('fs'),path=require('path'),root=process.cwd();
          const sharedComps=['AccountLink','AuthShell','LiveTripMap','NotificationBell','PageHeader','PushNotifications'];
          const sharedLibs=['supabaseClient','theme'];
          const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()?walk(p):(/\.(js|jsx|ts|tsx)$/.test(e.name)?[p]:[])});
          const rel=(from,to)=>{let r=path.relative(path.dirname(from),to).replaceAll(path.sep,'/');return r.startsWith('.')?r:`./${r}`};
          for(const file of [...walk(path.join(root,'apps')),...walk(path.join(root,'shared'))]){
            let s=fs.readFileSync(file,'utf8');
            for(const n of sharedComps){const r=rel(file,path.join(root,'shared/components',n));const e=n.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');s=s.replace(new RegExp(`from\\s+["'](?:\\.\\.?/)+app/components/${e}["']`,'g'),`from "${r}"`).replace(new RegExp(`from\\s+["'](?:\\.\\.?/)+components/${e}["']`,'g'),`from "${r}"`);}
            for(const n of sharedLibs){const r=rel(file,path.join(root,'shared/lib',n));const e=n.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');s=s.replace(new RegExp(`from\\s+["'](?:\\.\\.?/)+app/lib/${e}["']`,'g'),`from "${r}"`).replace(new RegExp(`from\\s+["'](?:\\.\\.?/)+lib/${e}["']`,'g'),`from "${r}"`);}
            s=s.replace(/from\s+["'](?:\.\.\/)+app\/lib\/admin["']/g,'from "../lib/admin"');
            fs.writeFileSync(file,s);
          }
          NODE

          # Customer-only guard was intentionally kept in Customer after the shared/common split.
          perl -0pi -e 's#from ["\x27]\.\./\.\./\.\./app/components/DateTimeBookingGuard["\x27]#from "./components/DateTimeBookingGuard"#g' apps/customer/app/layout.js
          # Customer-only components are already local; keep any direct local imports intact.

          for app in customer driver; do
            printf 'export { default } from "../../../shared/auth/login";\n' > "apps/$app/app/login/page.js"
            printf 'export { default } from "../../../shared/auth/forgot-password";\n' > "apps/$app/app/forgot-password/page.js"
            printf 'export { default } from "../../../shared/auth/reset-password";\n' > "apps/$app/app/reset-password/page.js"
          done
          for app in customer driver admin; do perl -0pi -e 's#@import "\.\./\.\./\.\./app/globals\.css";#@import "../../../shared/styles/globals.css";#g; s#@import "\.\./\.\./\.\./app/brand-theme\.css";#@import "../../../shared/styles/brand-theme.css";#g' "apps/$app/styles/globals.css"; done
          cat > apps/customer/app/api/_lib/roadDistance.ts <<'EOF'
          export { getRoadDistance } from "../../../../../shared/api/roadDistance";
          export type { RoadDistanceResult } from "../../../../../shared/api/roadDistance";
          EOF
          cat > apps/driver/app/api/_lib/roadDistance.ts <<'EOF'
          export { getRoadDistance } from "../../../../../shared/api/roadDistance";
          export type { RoadDistanceResult } from "../../../../../shared/api/roadDistance";
          EOF
          rm -rf app lib
          git add -A
          if git grep -nE 'from ["\x27].*app/(lib|components)/' -- apps shared; then echo 'ERROR: stale root-app imports remain.'; exit 1; fi
          if [ -d app ] && find app -type f -print -quit | grep -q .; then echo 'ERROR: root app still contains files.'; exit 1; fi
          if [ -d lib ] && find lib -type f -print -quit | grep -q .; then echo 'ERROR: root lib still contains files.'; exit 1; fi

      - name: Build all three applications
        shell: bash
        run: |
          set -euo pipefail
          for app in customer driver admin; do cd "apps/$app"; npm install --no-audit --no-fund; npm run build; cd ../..; done

      - name: Commit migration and remove temporary workflows
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f .github/workflows/voynu-source-separation.yml .github/workflows/voynu-source-separation-v2.yml
          git add -A
          git commit -m 'refactor: complete three-app source separation'
          git push origin HEAD:main
